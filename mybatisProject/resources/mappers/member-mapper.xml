<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
  PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
  "https://mybatis.org/dtd/mybatis-3-mapper.dtd">
  
<mapper namespace="memberMapper">
	<!-- namespace : 해당 mapper 파일의 고유 별칭 -->
 	<!-- 
 		* DML문의 경우
 		 <insert | update | delete id="식별자" [parameterType="전달받을 javaType(플클래스명 | 별칭)"]>
 		 
 		 </insert | update | delete>
 		 
 		* Select문의 경우
 		<select id="식별자" [parameterType="전달받을 javaType(플클래스명 | 별칭)"] resultType="조회결과 반환 javaType" | resultMap="조회결과 뽑아 매핑할 id">
 			? 대신 해당sql문에 전달된 객체로부터 값을 꺼낼 때 #{필드명 또는 키값}
 		</select>
 	 -->
 	 
 	 <!-- mybatis-config의 typealiases에 등록하고 사용해야 함 
 	 	  등록 후, ?를 #{필드명}으로 대체해주기.
 	 -->
 	 <insert id="insertMember" parameterType="Member">
 	 insert 
	  into member
	    (
	       user_no
	     , user_id
	     , user_pwd
	     , user_name
	     , email
	     , birthday
	     , gender
	     , phone
	     , address
	     )
	  values
	    (
	       seq_uno.nextval
	     , #{userId}
	     , #{userPwd}
	     , #{userName}
	     , #{email}
	     , #{birthday}
	     , #{gender}
	     , #{phone}
	     , #{address}
	     )
 	 </insert>
 	 
 	 
</mapper>